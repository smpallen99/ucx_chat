.content
  .list-view.channel-settings
    .title
      %h2 Room Info
    %form
      %ul.list.clearfix
        - for field <- @channel do
          = flex_form_line(field)
    - if has_permission? @current_user, "delete-" <> Permission.room_type(@channel_type) do
      %nav
        %button.button.danger.delete(title="#{~g(Delete)}")
          %i.icon-trash
